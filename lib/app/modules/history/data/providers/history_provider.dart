// Re-export attendance provider since history uses the same API
export '../../../attendance/data/providers/attendance_provider.dart';

// History-specific provider methods can be added here if needed
// For now, history uses AttendanceProvider.getHistory() method
